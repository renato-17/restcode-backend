#PostgreSQL Local DataSource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/restcodebd
spring.datasource.username=postgres
spring.datasource.password=70874160

#PostgreSQL Dialect Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.aopalliance.type=TRACE
spring.jackson.serialization.fail-on-empty-beans=false

# Swagger
springdoc.api-docs.path=/restcode-api-docs
springdoc.swagger-ui.path=/restcode-api-docs-ui.html

